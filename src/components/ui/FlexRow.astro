<div class="flex-row">
  <slot />
</div>

<style>
  .flex-row {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  .flex-row > .card {
    flex: 1 1 300px; /* Автоподстройка ширины */
    max-width: 400px;
  }
</style>
