<div class="flex-row">
  <slot />
</div>

<style>
  .flex-row {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    justify-content: center;
    align-items: stretch;
  }

  .flex-row > * {
    flex: 1 1 300px;
    max-width: 400px;
    min-width: 280px;
  }

  @media (max-width: 48rem) {
    .flex-row {
      gap: 1rem;
      flex-direction: column;
    }

    .flex-row > * {
      min-width: auto;
      max-width: 100%;
    }
  }
</style>
