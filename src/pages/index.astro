---
// pages/index.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import Button from '../components/ui/Button.astro';
import Card from '../components/ui/Card.astro';
---

<BaseLayout>
  <!-- Hero секция -->
  <section class="hero">
    <div class="hero-content">
      <h1>Opetellaan Suomea Yhdessä!</h1>
      <p>Изучайте финский язык с заботой и вниманием, как в долине Муми-троллей</p>
      <div class="hero-buttons">
        <Button href="/lessons" variant="filled">Начать учиться</Button>
        <Button href="/contact" variant="primary">Бесплатный урок</Button>
        <Button href="/about" variant="minimal">Узнать больше</Button>
      </div>
    </div>
    <div class="hero-image">
      <!-- Moomin-inspired illustration -->
      <div class="hero-illustration">
        <span>🎓📚🇫🇮</span>
      </div>
    </div>
  </section>

  <!-- Навигационные карточки -->
  <section class="features">
    <div class="feature-grid">
      <Card href="/lessons" class="feature-card">
        <div class="card-icon">📚</div>
        <h3>Интерактивные уроки</h3>
        <p>Пошаговые курсы от A1 до B2</p>
        <span class="card-link">Изучать →</span>
      </Card>

      <Card href="/grammar" class="feature-card">
        <div class="card-icon">🔍</div>
        <h3>Грамматика</h3>
        <p>15 падежей и спряжения глаголов</p>
        <span class="card-link">Изучать →</span>
      </Card>

      <Card href="/dictionary" class="feature-card">
        <div class="card-icon">📖</div>
        <h3>Словарь</h3>
        <p>Тематические подборки слов</p>
        <span class="card-link">Изучать →</span>
      </Card>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    padding: 4rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-content h1 {
    font-family: var(--font-heading);
    font-size: 3rem;
    color: var(--accent-primary);
    margin-bottom: 1rem;
    font-weight: 700;
  }

  .hero-content p {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-image {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-illustration {
    background: var(--accent-soft);
    border-radius: var(--border-radius-lg);
    height: 300px;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 4rem;
    border: 2px solid var(--accent-light);
  }

  .features {
    padding: 4rem 2rem;
    background: var(--background-light);
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .card-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    font-size: 1.25rem;
    font-weight: 600;
  }

  .feature-card p {
    color: var(--text-secondary);
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  @media (max-width: 768px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 2rem 1rem;
    }

    .hero-content h1 {
      font-size: 2rem;
    }

    .hero-buttons {
      justify-content: center;
    }

    .hero-illustration {
      height: 200px;
      font-size: 3rem;
    }

    .features {
      padding: 2rem 1rem;
    }
  }
</style>
